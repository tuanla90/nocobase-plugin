{
  "name": "@ptdl/plugin-field-order",
  "version": "0.1.0",
  "displayName": "Field Order",
  "description": "Reorder a collection's fields from the Collection Manager. Adds a 'Reorder fields' button to the Configure-fields drawer that opens a drag-and-drop list and persists the new order to fields.sort (drives this screen + new blocks' default field order). Runs on both the classic (/) and modern (/v/) clients.",
  "keywords": [
    "nocobase"
  ],
  "license": "Apache-2.0",
  "main": "./dist/server/index.js",
  "devDependencies": {
    "@nocobase/client": "2.x",
    "@nocobase/client-v2": "2.x",
    "react": "18.x"
  },
  "peerDependencies": {
    "@nocobase/client": "2.x",
    "@nocobase/client-v2": "2.x",
    "@nocobase/server": "2.x",
    "@nocobase/test": "2.x"
  }
}
